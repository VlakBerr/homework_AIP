#include <iostream>

int main() {
	int a1;
	int a2;
	int y1;
	int y2;
	std::cout << "Enter coordinats for horse: ";
	std::cin >> a1 >> a2;
	std::cout << "Enter coordinats for other figur: ";
	std::cin >> y1 >> y2;
	if (((a1 + 3) == y1 && (a2 + 1) == y2) || ((a1 + 3) == y1 && (a2 - 1) == y2) || ((a1 - 3) == y1 && (a2 + 1) == y2) || ((a1 - 3) == y1 && (a2 - 1) == y2) || ((a1 + 1) == y1 && (a2 + 3) == y2) || ((a1 + 1) == y1 && (a2 - 3) == y2) || ((a1 - 1) == y1 && (a2 + 3) == y2) || ((a1 - 1) == y1 && (a2 - 3) == y2)) {
		std::cout << "Kill";
	}
	else {
		std::cout << "Not Kill";
	}
	return 0;
}

-----------------------------


#include <iostream>
#include <string>

struct Student {
	std::string name;
	int age;
	double average;

	int main() {
		std::cout << "Name: " << name << ". Age: " << age << ". Average mark: " << average << ".\n";
		return 0;
	}
};

int main() {
	Student students[3];

	students[0] = { "Alex", 18, 5.0 };
	students[1] = { "Maria", 20, 5.1 };
	students[2] = { "Vacia", 10, 2 };

	students[0].main();
	students[1].main();
	students[2].main();




	return 0;
}


